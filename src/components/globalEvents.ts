const startEvent = new Event('start');
const stopEvent = new Event('stop');
const nightEvent = new Event('night');
const dayEvent = new Event('day');

export { startEvent, nightEvent, dayEvent, stopEvent };
